# Dementia MCP Environment Configuration

# ============================================================================
# DATABASE (Required)
# ============================================================================
# PostgreSQL/Neon connection string
# Format: postgresql://user:password@host:port/database?sslmode=require&sslnegotiation=direct
# Note: sslnegotiation=direct improves connection speed to Neon databases
DATABASE_URL=postgresql://user:password@hostname.neon.tech/dementia?sslmode=require&sslnegotiation=direct

# Optional: Specific database schema (auto-detected from project by default)
# DEMENTIA_SCHEMA=myproject

# ============================================================================
# AUTHENTICATION (Required for Cloud Hosting)
# ============================================================================
# API key for bearer token authentication
# Generate with: openssl rand -hex 32
# or: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
DEMENTIA_API_KEY=your-secure-api-key-here

# ============================================================================
# CLOUD SERVICES (Optional - for embeddings/LLM features)
# ============================================================================
# Voyage AI for embeddings (semantic search)
VOYAGEAI_API_KEY=your-voyage-api-key

# OpenRouter for LLM summarization
OPENROUTER_API_KEY=your-openrouter-api-key

# ============================================================================
# SERVER CONFIGURATION (Optional)
# ============================================================================
# Environment: production | staging | development
ENVIRONMENT=production

# Logging level: DEBUG | INFO | WARNING | ERROR
LOG_LEVEL=INFO

# HTTP port (DigitalOcean will override this)
PORT=8080

# ============================================================================
# FEATURE FLAGS (Optional)
# ============================================================================
# Provider selection (defaults shown)
EMBEDDING_PROVIDER=voyage_ai
EMBEDDING_MODEL=voyage-3.5-lite
EMBEDDING_DIMENSIONS=1024

LLM_PROVIDER=openrouter
LLM_MODEL=anthropic/claude-3.5-haiku
