# Claude Dementia MCP Server Requirements
mcp>=0.1.0

# Database adapters
psycopg2-binary>=2.9.9  # PostgreSQL adapter (binary for easier install)
python-dotenv>=1.0.0     # Load .env files

# Cloud hosting (HTTP transport)
fastapi>=0.115.0         # HTTP API framework
uvicorn[standard]>=0.32.0  # ASGI server for production
starlette>=0.37.0        # ASGI framework (FastAPI dependency)

# Production logging and monitoring
structlog>=24.1.0        # Structured logging
prometheus-client>=0.19.0  # Prometheus metrics

# Cloud API services
httpx>=0.27.0            # HTTP client for Voyage AI / OpenRouter